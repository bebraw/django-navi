from fabric.api import local

# TODO

def run_demo():
    # TODO: set up virtualenv if not set already and run the app
    # TODO: install navi to virtualenv (use pip too!)
    #local('cd demo && python manage.py runserver', capture=False)
    pass
